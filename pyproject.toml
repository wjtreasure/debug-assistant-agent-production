[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "debug-assistant-agent"
dynamic = ["version"]
description = "Read-only repository diagnosis agent with a production-style agent harness and SWE-bench localization evaluation."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27,<1", "pydantic>=2.7,<3", "python-dotenv>=1.0,<2", "numpy>=1.26,<3"]

[project.optional-dependencies]
eval = ["pandas>=2.1", "pyarrow>=15", "datasets>=3"]
dev = ["pytest>=8", "pytest-cov>=5"]
semantic = ["faiss-cpu>=1.8"]

[project.scripts]
debug-assistant = "debug_assistant.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "debug_assistant.__version__"}

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
